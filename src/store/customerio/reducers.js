import * as types from "./constants";

const initialState = {
  devices: [],
};

export default customerIOReducer = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};
